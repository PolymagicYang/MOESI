//
// Created by yanghoo on 2/25/24.
//

/*
#include "CPU.h"

int CPU::read(uint64_t addr) {
    log("cpu", "reading from address", addr);
    auto ret = this->cache->cpu_read(addr);
    log(name(), "read done");
    return ret;
}

int CPU::write(uint64_t addr) {
    log(name(), "writing to address", addr);
    return this->cache->cpu_write(addr);
}
*/
